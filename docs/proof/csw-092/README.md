# CSW-092 runtime proof

Current-head, redacted Linux runtime evidence for PR #995 is recorded here.
The captured files are generated by the repository's supported OpenClaw Bay
Playwright proof and use a fully synthetic status fixture. They prove the real
local Worker route and Bay page rendering, not production telemetry or
production performance.

See `proof-summary.json` for the exact current head, environment, assertions,
request classification, and fixture checksum. `runtime-transcript.md` records
the exercised surfaces, observations, and fixture limits.
`01a-mini-control-board.jpg` is a representative current-head frame of the
populated aggregate card.
